# ===================== Environment Configuration =====================

# Port number for the backend server
PORT=5000

# MongoDB URI for connecting to the local database
MONGO_URI=mongodb://localhost:27017/YouTube  # Connects to the 'YouTube' database on the local machine

# Secret key for signing JWT tokens (used for authentication)
JWT_SECRET=jwtSecretKey  # This key is used to generate and verify JWT tokens for secure authentication
